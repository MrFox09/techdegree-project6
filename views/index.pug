extends layout.pug

block content 
  article.grid-container.portfolio-intro
    div

      
      h1 Hey there, welcome to my Portfolio!
      p Let's make the web great together!

       
      p.lead.text-light        
        | I am learning Full Stack Javascript Deveolpement and this are the projects I made so far. For more details click on each project and have fun ;) . There is also a Live Demo button, make sure to check it out.
  article.grid-container.portfolio-index
    
    .grid-x.grid-margin-x.small-up-2.medium-up-2.large-up-3

      
    each project in projects
      .cell
        a(href=`/project/${project.id}`)
          img.thumbnail(src=`${project.image_urls[0]}`)
          h5 #{project.project_name}
      
